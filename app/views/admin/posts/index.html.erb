<div class='container px-5 px-sm-0'>
  <%= flash[:notice] %>
  <div class="col-12 mx-auto">
    <div class="d-flex mt-3">
      <h2>投稿一覧</h2>
      <h4 class="mt-2 ml-3"><%= link_to "画像一覧", admin_images_path %></h4>
    </div>
    <%= render 'admin/shared/post_index', posts: @posts %>
    <%= paginate @posts %>
  </div>
</div>